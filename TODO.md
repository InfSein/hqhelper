1. 该项目目前对于字体大小方面的处理很分散，有的是全局class（font-big/small）、有的是tailwind（text-xs）、有的是内部运算（calc）。除此之外，src\App.vue虽然实现了用户设置（naiveUIThemeOverrides），但只针对naive_ui的配置，很多其他地方没有照顾到。现在希望统一使用tailwind来控制，请规划实现方案。
2. 现在需要对工作流页面内的物品右键菜单（src\composables\useItemContextMenu.ts）增加两个选项，一个是在“加入到工作流”下方增加一个“添加到当前工作流”，效果和页面内的同名按钮一致；一个是“反查配方”按钮，点击后会搜索使用该物品作为素材的配方，并像搜索一样显示在制作笔记的的配方列表中、并记录搜索记录。这两个选项的业务逻辑不复杂，但是工作流页面本身后续路由可能发生变动，因此不能按路由写死，要考虑更加有弹性的策略。请规划实现方案。