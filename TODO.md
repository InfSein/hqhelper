- 调整ItemPop中，“装备属性”区域标题中的HqSwitcher，将其位置移动到ItemInfoHeader中物品主名称的右侧。在UI方面，移除NQ/HQ字样和切换示意图标，只保留最左侧的logo，并让其大小与物品主名称一致；在动画方面，将原先有些夸张的翻转特效改为简单的线段效果（HQ变为NQ时，从logo左上角向着右下角画一条线段；NQ变为HQ则是擦除）；是否显示仍保持与原先一致，即仅在“装备属性”区域显示时才显示；逻辑等则与原先保持一致。同时，排查在引用HqSwitcher期间的逻辑链，看看是否有重构优化的空间。请分析代码，生成实现方案。
