1. 现在需要对工作流页面内的物品右键菜单（src\composables\useItemContextMenu.ts）增加两个选项，一个是在“加入到工作流”下方增加一个“添加到当前工作流”，效果和页面内的同名按钮一致；一个是“反查配方”按钮，点击后会搜索使用该物品作为素材的配方，并像搜索一样显示在制作笔记的的配方列表中、并记录搜索记录。这两个选项的业务逻辑不复杂，但是工作流页面本身后续路由可能发生变动，因此不能按路由写死，要考虑更加有弹性的策略。请规划实现方案。